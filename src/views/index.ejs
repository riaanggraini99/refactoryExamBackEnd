<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
</head>
<body>
    <div class="container">
        <form action="/add" method="post">
        <legend>
            add a item
        </legend>
        <div class="form-group">
            <input type="text" id="title" class="form-control" name="title">
        </div>
        <div class="form-group">

            <textarea name="form-control" id="8" cols="1" name="description" rows="50"></textarea>
        </div>
        <button class="btn btn-primary">add</button>
        </form>
        <hr>
        <table class="table">
            <thead>
                        <tr>
                                <th>#</th>
                                <th>title</th>
                                <th>Description</th>
                                <th>aksi</th>
                    
                            </tr>
            </thead>
            <tbody>
                <% for(var i = 0; i < tasks.length; i++) { %>

                <tr>
                    <td><%= 1 + 1%></td>
                    <td>
                        <strong>
                            <%= tasks[i].title%>
                        </strong>
                    </td>
                    <td>
                        <%= tasks[i].description%>
                    </td>
                    <td>
                        <a class="<%= tasks[i].status ? 'btn btn-success':'btn btn-dark'%>"
                         href="/turn/<%=tasks[i]._id %>">Done</a>
                        <a class="btn btn-danger" href="/delete/<%= tasks[i]._id%>">Delete</a>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </hr>
    </div>
    
<script> 
document.getElementById('title').focus();
document.getElementById('delete').addEventListener('click',function functionName(){
let response = confirm('are you sure to be delete?');
if (!response){
    e.preventDefault();

}
})
</script>

</body>
</html>